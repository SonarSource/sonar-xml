<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.example.myapp">
  <uses-sdk android:minSdkVersion="any"/>

  <!-- Noncompliant@+1 {{Make sure allowing clear-text traffic is safe here.}} -->
    <application android:usesCleartextTraffic="true">
<!-- ^^^^^^^^^^^ -->
    </application>

  <!-- Compliant, minSdkVersion can not be resolved and maxSdkVersion not set -->
  <application>
  </application>

</manifest>
